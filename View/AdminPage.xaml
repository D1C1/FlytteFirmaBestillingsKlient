<Page
    x:Class="FlytteFirmaBestillingsKlient.AdminPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlytteFirmaBestillingsKlient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    RequestedTheme="light">

    <Grid>
        <StackPanel Margin="0,140,0,75">
            <ListView SelectedItem="{x:Bind ViewModelAdmin.SelectedBooking, Mode=TwoWay}" x:Name="Bookinglist" ItemsSource="{x:Bind ViewModelAdmin.OC_Bookings }" Margin="0,0,910,0">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Booking">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{x:Bind BookingId, Mode=TwoWay}"/>
                            <TextBox Text="{x:Bind BookingBruger.Navn, Mode=TwoWay}"/>
                            <TextBox Text="{x:Bind FlytteDato, Mode=TwoWay}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel DataContext="{Binding SelectedItem, ElementName=Bookinglist}"
            Margin="0,24,0,0">
                <TextBlock Text="{Binding Detaljer}"/>
            </StackPanel>
        </StackPanel>
        <Button Command="{x:Bind ViewModelAdmin.HentDataButton, Mode=OneWay}" Content="Hent Data" Margin="0,500,0,0" VerticalAlignment="Top" Height="92" Width="237" HorizontalAlignment="Center"/>
        <HyperlinkButton Content="Click to go to MainPage"
                 Click="HyperlinkButton_Click" Margin="1342,10,0,0" VerticalAlignment="Top"/>
        <Button Command="{x:Bind Mode=OneWay, Path=ViewModelAdmin.SletData}" Content="Slet Booking" Margin="0,628,0,0" VerticalAlignment="Top" Height="92" Width="237" HorizontalAlignment="Center"/>
        <Button Command="{x:Bind Mode=OneWay, Path=ViewModelAdmin.GemData}" Content="Gem Liste" Margin="1230,628,0,0" VerticalAlignment="Top" Height="92" Width="237"/>
    </Grid>
</Page>
